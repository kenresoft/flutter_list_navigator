name: flutter_list_navigator
description: Jump to Item Flutter Package which contains a navigator that can be used to jump to a particular index of a list.
version: 0.0.1
homepage: ''

environment:
  sdk: '>=3.0.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  # flutter_riverpod: ^2.3.6

flutter:


